SELECT
    publishername,
    COUNT(DISTINCT A.UPW_DOI) as N
FROM 
    WOS_NULLS A
INNER JOIN
    WOS_B_2019.ISSUES B ON B.ISSN = WOS_JOURNAL_ISSNS
INNER JOIN
WOS_B_2019.PUBLISHERS C ON C.PK_PUBLISHERS = B.FK_PUBLISHERS

GROUP BY C.PUBLISHERNAME
ORDER BY COUNT(DISTINCT A.UPW_DOI) DESC
